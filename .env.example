# SQLite is created locally by `prisma db push`
DATABASE_URL="file:./prisma/dev.db"

# Optional: AI planner / TTS (template + espeak-ng / silence fallbacks work without this)
OPENAI_API_KEY=sk-your-openai-api-key
# OPENAI_MODEL=gpt-4o
