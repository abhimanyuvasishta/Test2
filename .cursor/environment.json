{
  "name": "AI Video Creator",
  "install": "npm ci && npx prisma generate && npx prisma db push",
  "terminals": [
    {
      "name": "Next.js",
      "command": "npm run dev",
      "description": "Client Video Studio dev server on port 3000"
    }
  ],
  "ports": [
    {
      "name": "web",
      "port": 3000
    }
  ]
}
